<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.statsgaa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <activity android:name=".Register"></activity>
        <activity android:name=".addFixtures"></activity>
        <activity android:name=".addNotes"></activity>
        <activity android:name=".addPlayers"></activity>
        <activity android:name=".EditDataActivity"></activity>
        <activity android:name=".Fixtures"></activity>
        <activity android:name=".ListDataActivity"></activity>
        <activity android:name=".LoggedIn"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".Notes"></activity>
        <activity android:name=".Players"></activity>


    </application>

</manifest>